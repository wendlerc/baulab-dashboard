[project]
name = "gpu-dashboard"
version = "0.1.0"
description = "GPU utilization dashboard across SSH-accessible nodes"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
]

